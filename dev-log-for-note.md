# DEEP SEA LAB 開発記録

## はじめに

「DEEP SEA LAB」は、深海の研究所を舞台にした、ドット絵風のインタラクティブWebサイトです。3人の研究員キャラクターがいて、タップすると喋り、音楽が流れ、動画や写真を一緒に見れる——そんな「居場所」のようなサイトを作りました。

草案（コンセプト・キャラ設定・世界観）はChatGPT（GPTs）で練り、その後の実装はClaude Code（Anthropic）で行いました。

---

## 制作の流れ

### Phase 1: 素材準備とフォルダ構成

**やったこと：**
- GitHubリポジトリの作成
- 画像素材のアップロード（ロゴ、背景マップ、キャラクター立ち絵）
- フォルダ構成の整理

**フォルダ構成：**
```
public/assets/
  ├── logo/          ← ラボのロゴ
  ├── maps/          ← 部屋の背景（居間・司令室）
  ├── characters/    ← 研究員A・B・Cの立ち絵
  └── audio/         ← テーマ曲（BGM）
```

最初はファイル名が大文字小文字混在していたり、余計なファイルがあったりしてバタバタしました。Claudeにファイル名の正規化と整理をお願いして解決。

---

### Phase 2: MVP（最小限の動くもの）を一気に実装

**Claudeへの指示（ざっくり）：**
> 背景画像の上にキャラクターを配置して、タップしたら吹き出しでセリフを表示するサイトを作って

**できたもの：**
- 背景画像の上にキャラ3人を配置
- キャラをタップ → ランダムにセリフが吹き出し表示
- 「居間」「司令室」のルーム切り替えボタン
- GitHub Pagesへのデプロイ設定

ここまでが最初の大きな一歩。HTMLファイル1つ、CSSファイル1つ、JSファイル1つというシンプル構成で、フレームワークは使わず素のHTML/CSS/JavaScriptで組みました。

---

### Phase 3: セリフの充実

**Claudeへの指示：**
> 各キャラのセリフを20個ずつに増やして。性格が出るようにして

**3人の個性：**
- **研究員A**：明るくてノリが良い。お嬢ちゃん呼び。場を盛り上げるムードメーカー
- **研究員B**：冷静で知的。データや分析が好き。でも仲間への信頼は厚い
- **研究員C**：寡黙で頼れる存在。短い言葉で核心を突く。背中で語るタイプ

合計60個のセリフが、タップするたびにランダムで表示されます。

---

### Phase 4: キャラ配置の微調整

**起きた問題：**
> 研究員Bがテーブルの上に立ってるように見える

**修正：**
- Bのポジションを下に調整（床に立たせる）
- さらに右にずらしてテーブルとの重なりを回避

地味だけど、見た目の違和感をなくす大事な作業でした。

---

### Phase 5: メディアプレイヤーの実装

**Claudeへの指示（ざっくり）：**
> YouTube、SUNO AI、動画・写真・音声ファイルを再生できるメディアプレイヤーを追加して。キャラが見てるリアクションも欲しい

**できたもの：**
- 4つのタブ切り替え（URL / 動画 / 写真 / ファイル）
- YouTube URLを貼ると埋め込みプレイヤーで再生
- SUNO AIのURLにも対応（CDN経由での音声再生を試みる）
- 端末内の動画・写真・音声ファイルを選択して再生
- メディア再生中、キャラクターが「おっ、何見るんだ？」等のリアクション

---

### Phase 6: テーマ曲（BGM）の実装

**Claudeへの指示：**
> テーマ曲をループ再生したい。メディア再生時はフェードアウトして、終わったらフェードインで戻して

**仕組み：**
- ページを開くとBGMが自動ループ再生
- 動画や音声を再生 → BGMが0.8秒かけてフェードアウト
- メディアが終了 or 閉じる → BGMが0.8秒かけてフェードイン
- 写真を貼った時はBGMを止めない（後で追加した改善）

---

### Phase 7: スプラッシュ画面

**Claudeへの指示：**
> サイトを開いた時、最初にロゴが表示されて「TAP」と出る画面を作って。タップしたらグリッチエフェクトでノイズが走って、ラボの画面に切り替わるようにして

**できたもの：**
1. 暗い画面にロゴが浮かぶ → 「TAP」が点滅
2. タップすると：
   - ロゴがグリッチ（ちらつき）アニメーション
   - 砂嵐のようなノイズエフェクト（Canvas描画）がピークまで上がる
   - ノイズが晴れるとラボが現れる
   - 同時にBGMが開始
3. 全体の演出が約2.5秒

このスプラッシュ画面は一度リワーク（作り直し）しています。最初のバージョンは単純なフェードでしたが、「ノイズが走ってから画面が現れる」演出に変更しました。

---

### Phase 8: 細かい改善

- URLの入力欄のプレースホルダーを分かりやすく修正
- SUNO AIのURL直接再生ができない旨の注意書きを追加
- 写真を表示した時にBGMが止まってしまう問題を修正

---

## 技術スタック

- **HTML/CSS/JavaScript**（フレームワークなし、素のVanilla JS）
- **Vite**（ビルドツール。ただし実質的にはビルド不要の静的サイト）
- **GitHub Pages**（ホスティング）
- **GitHub Actions**（自動デプロイ）

---

## Claudeとの開発で感じたこと

### 良かったこと
- 「こういう感じにして」という曖昧な指示でも、意図を汲んで実装してくれた
- 一度に多くの機能を実装できるスピード感
- バグ報告をすると原因を特定して修正してくれる
- GitHubへのデプロイ設定まで一貫して対応

### 注意点
- 会話が長くなると過去のやり取りが圧縮されて、文脈が消えることがある
- 重要な仕様や設計判断は、CLAUDE.mdなどに記録しておくと安心
- スクリーンショットを見せてフィードバックするのが効果的だった

---

## まとめ

- 草案・コンセプト設計：**ChatGPT（GPTs）** で壁打ち
- 実装・デプロイ：**Claude Code** で一気に構築
- 制作期間：**約2日**（2026年3月16日〜17日）

GPTsで世界観を練って、Claudeで実装する。この「AIリレー」スタイルは、アイデアと実装を分けて考えられるので、個人的にとても合っていました。

サイトURL: https://nanashi925.github.io/DEEP-SEA-LAB-Claude/
